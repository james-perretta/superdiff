#! /usr/bin/env python3

from setuptools import setup

setup(name='superdiff',
      version='1.0.0a0',
      description="Extends Python's difflib to allow more flexible diffing",
      author='James Perretta',
      author_email='jameslp@umich.edu',
      packages=["superdiff"],
      install_requires=[])
